/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "blue-jean-store-admin-1h0nvp5kn-01codexs-projects.vercel.app"],
  },
};

export default nextConfig;
